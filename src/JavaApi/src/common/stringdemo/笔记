
String

相同对象的哈希码 一定相等 ，哈希码相等的对象不一定是 相同的对象

“==”和“equals()”的区别：
1、“==”判断的是两个对象是不是同一个对象
2、“equals()”判断的是两个对象的内容是否相等


区别：
1、二者都是比较两个对象是不是同一个对象
2、当重写了 hashCode和equals方法之后，==比较的是两个对象是不是同一个对象
  equals比较的是两个对象的内容是不是一样
